spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: http://github.com/NextFeng/cloud_config.git
          username: 1225842878@qq.com
          passphrase: Http1024
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      expose: "*"
